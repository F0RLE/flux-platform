{
    "name": "flux-platform-frontend",
    "version": "1.2.0",
    "description": "Flux Platform Frontend",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "tauri:dev": "cd ../src-tauri && cargo tauri dev",
        "tauri:build": "cd ../src-tauri && cargo tauri build"
    },
    "devDependencies": {
        "@tauri-apps/api": "^2.0.0",
        "@types/node": "^25.0.3",
        "typescript": "^5.3.0",
        "vite": "^6.0.0"
    }
}
