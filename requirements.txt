# Minimal dependencies for Electron launcher only
# Bot/LLM/SD dependencies are installed separately when needed
psutil>=7.1.3
python-dotenv>=1.2.1
requests>=2.32.5
cryptography>=46.0.3
packaging>=25.0
SpeechRecognition
